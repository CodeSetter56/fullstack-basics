<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Notes App</title>
  </head>
  <body class="bg-zinc-900 text-white">
    <div class="main w-full min-h-screen p-10">
      <a href="/" class="text-xl p-2 bg-black">Go back</a>
      <!-- from getNote controller route -->
      <form class="mt-4" action="/notes/edit/<%= slug %>" method="POST">
        <input
          type="text"
          name="title"
          placeholder="Title"
          class="block w-full p-2 mb-4 bg-zinc-800 border border-zinc-700 rounded-md"
          value="<%= title %>"
        />
        <textarea
        class="block w-full p-2 mb-4 bg-zinc-800 border border-zinc-700 rounded-md"
        placeholder="Take a note..."
        name="details"
        rows="4"
        >
        <!-- fill the are with previous details from showEditNote controller -->
<%= details %></textarea
        >
        <button
          type="submit"
          class="px-4 py-2 text-lg bg-orange-600 hover:bg-blue-700 rounded-md"
        >
          Save Note
        </button>
      </form>
    </div>
  </body>
</html>
